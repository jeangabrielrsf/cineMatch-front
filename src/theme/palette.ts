import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    custom: {
      main: '#FB8500',
      light:'#fff',
      dark: '#FB8500',
      contrastText: '#FB8500'
    },
  },
});

export default theme