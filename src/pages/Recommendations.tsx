import { Box, Typography } from "@mui/material";

export default function Recommendations() {
    return (
        <Box>
            <Typography variant="h3">Recomendações</Typography>
        </Box>
    );
}
